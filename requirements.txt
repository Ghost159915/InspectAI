# ── Computer Vision ───────────────────────────────────────────────────────────
ultralytics>=8.3.0              # YOLOv8 detection + fine-tuning
opencv-python-headless>=4.10.0  # Headless OpenCV (no GUI deps — works in Docker / HF Spaces)
Pillow>=10.4.0                  # Image I/O and annotation drawing

# ── LLM & Agent ───────────────────────────────────────────────────────────────
langchain-core>=0.3.0           # PromptTemplate and base abstractions
langchain-community>=0.3.0      # Document loaders, FAISS wrapper, HF embeddings
langchain-ollama>=0.2.0         # Local LLM via Ollama (used when LLM_BACKEND=ollama)
langchain-text-splitters>=0.3.0 # RecursiveCharacterTextSplitter

# ── RAG / Vector Store ────────────────────────────────────────────────────────
faiss-cpu>=1.9.0                # Local vector similarity search (CPU build)
sentence-transformers>=3.3.0    # Embedding model for RAG (~80 MB, runs on CPU)

# ── Hugging Face ──────────────────────────────────────────────────────────────
# hf_hub_download — pulls model weights on HF Spaces if not present locally.
# InferenceClient — used when LLM_BACKEND=hf for serverless LLM calls.
huggingface_hub>=0.26.0

# ── UI ────────────────────────────────────────────────────────────────────────
gradio>=5.9.0                   # Web UI — served at localhost:7860

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
pydantic>=2.9.0
numpy>=1.26.0
tqdm>=4.67.0
PyYAML>=6.0
